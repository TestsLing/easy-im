filter:
  paths:
    - src/
  excluded_paths:
    - tests/*

checks:
  php: true

build:
  nodes:
    my-tests-with-coverage:
      tests:
        override:
          - command: phpunit --coverage-clover=coverage.clover
            coverage:
              file: coverage.clover
              format: php-clover
